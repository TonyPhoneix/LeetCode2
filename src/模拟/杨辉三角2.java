package 模拟;

import java.util.ArrayList;

/**
 * 给定一个索引k，返回Pascal三角形的第k行。
 * 例如，给定k = 3，
 * 返回[1,3,3,1]。
 * 注意：
 * 你可以优化算法，只使用O（k）额外的空间吗？
 * Created by Tony on 2017/6/8.
 */
public class 杨辉三角2 {
    public ArrayList<Integer> getRow(int rowIndex) {
        int[] dp = new int[rowIndex+1];
        dp[0] = 1;

        return null;
    }
}
